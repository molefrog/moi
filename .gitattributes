# Vendored React ESM — generated by scripts/build-vendor.ts (run `bun run
# vendor:react`). Mark as generated so GitHub collapses the diff and it's
# excluded from language stats; don't show a textual diff on regeneration.
client/vendor/** linguist-generated=true -diff
