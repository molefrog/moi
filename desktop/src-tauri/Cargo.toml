[package]
name = "moi-desktop"
version = "0.1.0"
description = "moi desktop shell"
edition = "2021"
license = "Elastic-2.0"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde_json = "1"
libc = "0.2"

[profile.release]
strip = true
lto = true
